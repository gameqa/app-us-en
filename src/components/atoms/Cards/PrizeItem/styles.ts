import { StyleSheet } from "react-native";

const style = StyleSheet.create({
	outer: {
		padding: 0,
		height: 290,
	},
	image: {
		width: "100%",
		height: 250,
	},
	pad: {
		padding: 10,
	},
});

export default style;
