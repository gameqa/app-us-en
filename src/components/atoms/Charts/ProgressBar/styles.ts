import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	bar: {
		height: 10,
		borderRadius: 5,
	},
	outer: {
		width: "100%",
	},
	barOuter: {
		width: "100%",
		backgroundColor: "#eee",
		borderRadius: 5,
	},
});

export default styles;
