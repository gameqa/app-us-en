import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	outer: {
		height: 40,
		width: "100%",
		borderWidth: 1,
		borderRadius: 5,
		borderColor: "#d1d1d1",
		padding: 7,
	},
});

export default styles;
