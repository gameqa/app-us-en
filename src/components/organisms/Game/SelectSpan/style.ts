import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	outer: {
		flex: 1,
	},
	instructions: {
		padding: 5,
		borderWidth: 1,
		borderColor: "#aaa",
		borderStyle: "dashed"
	}
});
