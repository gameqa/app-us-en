import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	para: {
		marginBottom: 10,
	},
	outer: {
		paddingBottom: 20,
	},
	outerCotnainer: {
		flex:1,
	},
});

export default styles;
