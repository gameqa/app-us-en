import { StyleSheet } from "react-native";

const style = StyleSheet.create({
	outer: {
		position: "absolute",
		zIndex: 20,
		left: 0,
		right: 0,
		marginHorizontal: 10,
	},
});

export default style;
