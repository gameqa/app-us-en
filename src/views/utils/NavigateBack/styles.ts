import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	backButton: {
		flexDirection: "row",
		marginTop: 20,
		marginBottom: 20,
	},
	backText: { marginLeft: 15 },
});

export default styles;
